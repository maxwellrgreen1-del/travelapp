# Supabase Dashboard → Settings → Project API (Project URL + anon/public key).
# Duplicate this file → `.env.local` and paste real values. Never commit `.env.local`.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
