import { LoginExperience } from "@/features/auth/LoginExperience";

/** Mock login sails — validates inputs client-side until Supabase auth ferries payloads. */
export function LoginScreen() {
  return <LoginExperience />;
}
