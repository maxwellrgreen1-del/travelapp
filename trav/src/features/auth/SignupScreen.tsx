import { SignupExperience } from "@/features/auth/SignupExperience";

/** Mock onboarding shell — rehearsals only; real accounts ship alongside Supabase. */
export function SignupScreen() {
  return <SignupExperience />;
}
